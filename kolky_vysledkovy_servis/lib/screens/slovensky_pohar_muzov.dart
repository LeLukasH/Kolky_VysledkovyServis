import 'package:flutter/material.dart';

class SlovenskyPoharMuzov extends StatelessWidget {
  const SlovenskyPoharMuzov({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Slovenský pohár mužov'),
      ),
    );
  }
}
