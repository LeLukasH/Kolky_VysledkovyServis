import 'package:flutter/material.dart';

class DLZapad extends StatelessWidget {
  const DLZapad({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DL Západ'),
      ),
    );
  }
}
