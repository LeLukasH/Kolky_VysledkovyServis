import 'package:flutter/material.dart';

class Liga3TN extends StatelessWidget {
  const Liga3TN({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('3. liga TN kraja'),
      ),
    );
  }
}
