import 'package:flutter/material.dart';

class ExtraligaZeny extends StatelessWidget {
  const ExtraligaZeny({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AGRO CS - RONA Extraliga ženy'),
      ),
    );
  }
}
