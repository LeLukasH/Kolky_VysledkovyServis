import 'package:flutter/material.dart';

class DLVychod extends StatelessWidget {
  const DLVychod({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DL Východ'),
      ),
    );
  }
}
