import 'package:flutter/material.dart';

class Liga1Zapad extends StatelessWidget {
  const Liga1Zapad({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('1.KL Západ'),
      ),
    );
  }
}
