import 'package:flutter/material.dart';

class Liga3TTNR extends StatelessWidget {
  const Liga3TTNR({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('3. liga TT + NR kraj'),
      ),
    );
  }
}
