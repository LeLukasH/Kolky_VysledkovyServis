import 'package:flutter/material.dart';

class ExtraligaMuzi extends StatelessWidget {
  const ExtraligaMuzi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Extraliga muži'),
      ),
    );
  }
}
